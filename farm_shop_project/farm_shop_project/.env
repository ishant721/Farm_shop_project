# .env file for Farm Shop Django Project

# Django Core Settings
# Generate a strong, unique secret key for production:
# python -c 'from django.core.management.utils import get_random_secret_key; print(get_random_secret_key())'
DJANGO_SECRET_KEY='your_super_secret_key_for_development_replace_this_in_production'
DJANGO_DEBUG=True
# Set to False in production. In production, also set ALLOWED_HOSTS in settings.py.

# Email Settings (for OTPs, Welcome, Birthday Emails)
# Configure for SMTP (e.g., Gmail, SendGrid, Mailgun)
# For Gmail, you'll need an 'App password' if you have 2FA enabled.
# Go to Google Account -> Security -> App passwords.
EMAIL_HOST='smtp.gmail.com'
EMAIL_PORT=587 
EMAIL_USE_TLS=True 
EMAIL_USE_SSL=False 

# !!! IMPORTANT: Replace with your actual sending email and app password/SMTP password !!!
EMAIL_HOST_USER='ishantsingh01275@gmail.com'
EMAIL_HOST_PASSWORD='wrms alha oxsq cbaa'

# Database Settings (Example for PostgreSQL. If using SQLite, these are not strictly needed.)
# DATABASE_ENGINE='django.db.backends.postgresql_psycopg2'
# DATABASE_NAME='your_db_name'
# DATABASE_USER='your_db_user'
# DATABASE_PASSWORD='your_db_password'
# DATABASE_HOST='localhost' # Or your DB host/IP
# DATABASE_PORT='5432'      # Or your DB port

# Twilio Settings (Uncomment and fill these if you enable messaging functionality)
# TWILIO_ACCOUNT_SID='ACxxxxxxxxxxxxxxxxxxxxxxxxxxxxx' # Your Twilio Account SID
# TWILIO_AUTH_TOKEN='your_twilio_auth_token'           # Your Twilio Auth Token
# TWILIO_PHONE_NUMBER='+1234567890'                    # Your Twilio SMS-enabled phone number (E.164 format)
# TWILIO_WHATSAPP_NUMBER='whatsapp:+1234567890'        # Your Twilio WhatsApp-enabled phone number (E.164 format with 'whatsapp:' prefix)